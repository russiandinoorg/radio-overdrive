import { PresenterListItem } from './PresenterListItem';
import { PresentersContainer } from './PresentersContainer';

export const Presenters = {
  Card: PresenterListItem,
  Container: PresentersContainer,
};
